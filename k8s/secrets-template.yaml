# DO NOT commit real secrets!
# Generate with: kubectl create secret generic jarvis-secrets --from-env-file=.env -n jarvis
apiVersion: v1
kind: Secret
metadata:
  name: jarvis-secrets
  namespace: jarvis
type: Opaque
# Add your keys via: kubectl create secret generic jarvis-secrets --from-env-file=.env -n jarvis
